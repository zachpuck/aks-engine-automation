name: debug
description: debug repo
inputs:
  srcDir:
    dir:
      default: .
run:
  serial:
    - container:
        image: { ref: 'golang:1.11-alpine3.8' }
        cmd:
          - sh
          - -ce
          - |
            echo "debug not implemented"
        dirs:
          /go/src/github.com/zachpuck/aks-engine-automation: $(srcDir)
        workDir: /go/src/github.com/zachpuck/aks-engine-automation
